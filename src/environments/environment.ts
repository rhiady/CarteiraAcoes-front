export const environment = {
  // The Angular development server proxies this path to localhost:8082,
  // avoiding browser CORS restrictions during local development.
  apiBaseUrl: '/api',
} as const;
